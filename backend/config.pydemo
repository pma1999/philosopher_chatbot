# If you have a default API key, uncomment the following line and add your key
ANTHROPIC_API_KEY = ""

# Alternatively, you can set this as an environment variable named ANTHROPIC_API_KEY

# Flask secret key for session encryption
SECRET_KEY = "your_secret_key_here"  # Change this to a random string in production

# Session configuration
SESSION_TYPE = "filesystem"
SESSION_PERMANENT = False
SESSION_USE_SIGNER = True

# Rate limiting configuration
RATELIMIT_DEFAULT = "200 per day, 50 per hour"